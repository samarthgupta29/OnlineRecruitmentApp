<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.samarth.careertest1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".realandroiddevelopment.MyFormActivity"
            android:label="@string/title_activity_my_form"
            android:theme="@style/AppTheme" />
        <activity android:name=".personal_info" />
        <activity android:name=".contact_details" />
        <activity android:name=".work_experience" />
        <activity android:name=".education"></activity>
    </application>

</manifest>